import 'package:flutter/material.dart';

// Texts of the app mobile

const titleApp = "EMI";
const namePageHome = "Escuela Militar de Ingenieria";
const visionPage =
    "El Ingeniero de Sistemas graduado de la EMI, es competente para realizar investigación, modelación, implementación, evaluación de sistemas usando nuevas tecnologías de información y comunicación.";
const misionPage =
    "El Ingeniero de Sistemas graduado de la EMI, es competente para realizar investigación, modelación, implementación, evaluación de sistemas usando nuevas tecnologías de información y comunicación.";

// Layouts and styles of the app mobile

const primaryColor = Color(0xFF8894bc);
const homePageLigthColor = Color(0xFFfbfcff);
const secondaryColor = Color(0xFF8B94BC);
const greenColor = Color(0xFF6AC259);
const redColor = Color(0xFFE92E30);
const grayColor = Color(0xFFC1C1C1);
const blackColor = Color(0xFF101010);
const blueColor = Color(0x144570);
const yellowColor = Color(0xF4B909);
const boxShadowColor = Color(0X364156);
const primaryGradient = LinearGradient(
  colors: [Color(0xFFA8AEC1), Color(0xFFB5D2CB)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);
const secondaryGradient = LinearGradient(
  colors: [Color(0xff11151C), Color(0xff212D40)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const thirdGradient = LinearGradient(
  colors: [Color(0xFF0D3D66), Color(0xFF0D3D90)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const double defaultPadding = 20.0;

const double bigFont = 36.0;
const double semiBigFont = 32.0;
const double mediumFont = 28.0;
const double semiMediumFont = 24.0;
const double smallFont = 20.0;
const double semiSmallFont = 16.0;
